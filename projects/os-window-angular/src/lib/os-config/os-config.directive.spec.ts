import { OsConfigDirective } from './os-config.directive';

describe('OsConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new OsConfigDirective();
    expect(directive).toBeTruthy();
  });
});
